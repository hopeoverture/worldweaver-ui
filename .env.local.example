# WorldWeaver Environment Configuration
# Copy this to .env.local and update with your actual values

# --- Supabase (Required) ---
# Found in Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Found in Supabase Dashboard → Settings → API (service_role)
# Used only by trusted server routes (e.g., admin seeder)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- Admin Seeder (Dev/Ops) ---
# Set a random string and pass it as header x-admin-token or ?token= when calling the seed endpoint
SEED_ADMIN_TOKEN=change-me-for-dev-only
# Base URL of the running app for seed script
SEED_BASE_URL=http://localhost:3000
# Gate the admin seed route in production (keep false unless you know what you're doing)
# ADMIN_SEED_ENABLED=false

# --- Local/Tests (Optional) ---
# Base URL for API test script (scripts/test-api-endpoints.js)
# API_BASE_URL=http://localhost:3000
# Test credentials for exercising authenticated API endpoints
# TEST_EMAIL=developer@yourdomain.com
# TEST_PASSWORD=your-password
# TEST_INVITE_EMAIL=invitee@yourdomain.com

# --- Notes ---
# NextAuth is NOT used. Auth is handled by Supabase via @supabase/ssr.
# DATABASE_URL is not required by the UI; use Supabase hosted Postgres.
# OPENAI_API_KEY is optional and not used yet.
